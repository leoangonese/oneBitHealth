import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    numberImc: {
        fontSize: 48,
        color: '#ff0043',
        fontWeight: 'bold'
    }
})
export default styles